import React from 'react'

const TodoItem = ({text}) => {

    return (
        < >

            <div  >
               {text}
            </div>
            <button >
                Delete
            </button>
        </>
    )
}

export default TodoItem
